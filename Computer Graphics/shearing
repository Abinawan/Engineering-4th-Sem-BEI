#include<stdio.h>
#include<graphics.h>
#include<math.h>
int main()
{
	int gd = DETECT, gm;
    initwindow(10000,10000);
    int s;
    //line(0,240,640,240);
   // line(320,0,320,480);
    //int x1,y1,x2,y2,choice;
    line(400,100,500,100);
    line(400,100,400,200);
    line(500,200,500,100);
    line(500,200,400,200);
//
	printf("Enter shearing coefficient: ");
	scanf("%d",&s);
	line(400+(s*100),100,500+(s*100),100);
	line(400+(s*100),100,400+(s*200),200);
	line(500+(s*200),200,500+(s*100),100);
	line(500+(s*200),200,400+(s*200),200);
	
    getch();
    closegraph();
    return 0;
    
}
